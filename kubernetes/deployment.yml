apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: site
  namespace: site
  labels:
    name: site
    role: web
spec:
  minReadySeconds: 30
  strategy:
    type: RollingUpdate
  replicas: 1
  template:
    metadata:
      name: site
      labels:
        name: site
        role: web
    spec:
      containers:
        - image: creasty/site:latest
          name: site
          ports:
            - containerPort: 8080
